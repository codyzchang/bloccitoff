<h1><%= @item.title %></h1>

<div class="row">
  <div class="col-md-4">
    <% if policy(@item).edit? %>
    <%= link_to "Edit", edit_list_item_path(@list, @item), class: 'btn btn-success' %>
    <% end %>
  </div>
</div>